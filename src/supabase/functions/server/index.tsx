// Server-only Supabase function entry moved to /server to avoid bundling by Vite.
// Keep a minimal stub in `src` so the client TypeScript server doesn't try to parse server-only code.
// This file is intentionally empty for the client build.
export {};